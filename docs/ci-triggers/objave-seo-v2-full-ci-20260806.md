# Objave SEO V2 full CI trigger

Purpose: force the repository's standard pull-request workflows to evaluate the latest generated `/objave/` metadata and schema commit after the materialization bot updated the branch.

This file contains no production configuration, secrets, routes, bindings, mail settings, or deploy instructions.
