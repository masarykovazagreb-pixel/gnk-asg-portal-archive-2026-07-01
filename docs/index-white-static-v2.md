# Luxury static index v2

- Ivory/white corporate design with black typography and brushed-gold details.
- Navigation is authored inside the index template and is not injected by another page shell.
- THE CODE remains a full-size iframe: desktop minimum 820 px, up to 1180 px, with full-screen, reload and separate-window controls only.
- Financial cards use FY2025 figures from the supplied reports.
- Global map renders country boundaries, six continent labels, 45 city/country/continent locations, red existing/planned markers and gold routes from Zagreb.
- The public root paths `/` and `/en/` are served from the new static templates by the Worker.
- News, publications, gallery and Admin remain separate existing destinations until the index is visually approved.

## Pending binary assets

The two supplied PDF files still need binary upload to the public `/documents/` paths referenced by the buttons. The index code must not claim successful PDF delivery until those files are present.
