# Handoff — 26-27.7.2026. → priprema za sutra

## Status na kraju dana: SVE ZDRAVO

- **1225 stranica**, 0 grešaka, 0 upozorenja (audit-public-portal-v1.mjs)
- **SEO integritet**: ok:true, samo 2 poznata manja upozorenja (duplicate title na vlastito-imenskim stranicama, nisu greška)
- **Digital Workforce API**: svih 13 endpointa zdravo (state/projects/risks/opinions/dependencies/tasks/credits/bulletins/newsroom/workers/log/comms/plan)
- **12 aktivnih automatizacija**: sve zadnje uspješno pokrenute danas
- **Mirror backup** (masarykova): `main` HEAD identičan izvoru, 896 grana na oba mjesta, sync svakih 6h radi

## Riješeno danas (kronološki, najvažnije)

1. Digital Workforce — puna engleska verzija (12 stranica, bilingual API, real slike)
2. Trgovina (`/trgovina/`) — katalog + zahtjev za ponudu (bez plaćanja), + product-catalog-api-v1.js (POST/PUT sa API ključem)
3. Backup mirror na `masarykovazagreb-pixel/gnk-asg-portal-archive-2026-07-01` — postavljen, testiran, radi svakih 6h
4. AKTUAL MEDIA — Digital Workforce sadržaj sad se pojavljuje ondje, jasno označen "GNK ASG · Simulacija"
5. **Popravljen bug koji sam ja unio**: DW stavke su privremeno "zamrznule" glavnu objavu na Aktual Media (items[0] uvijek DW umjesto svježe vijesti) — popravljeno, potvrđeno na produkciji
6. Sitemap regeneriran (406 stranica), GSC prioritetni vodič napisan (`docs/seo/url-submission-priority-2026-07-26.md`)
7. Amazon Associates prijava u tijeku (Store ID, opis sadržaja odgovoreno)

## ZA SUTRA — konkretni koraci

### 1. Prijenos GitHub Secrets: beckuphome-gnk → masarykova
**Pravilo dogovoreno danas: SAMO DODAJEMO nove, NE brišemo/ne diramo postojeće na trenutnom repou.**

**KLJUČNA NAPOMENA (otkriveno provjerom workflow konteksta):** `GNK_ASG_OPERATOR_TOKEN` se koristi u koraku "Resolve token hash after integrity verification" — GitHub Actions IZRAČUNAVA HASH te vrijednosti radi verifikacije PREMA SAMOM Cloudflare Workeru. To znači vrijednost ovog (i vjerojatno preostalih 2) secreta **mora biti identična** onoj koja je već postavljena na Cloudflare Workeru (`gnk-asg-direct-operator`) — **NE smije se regenerirati nasumično na GitHub strani**, jer bi to pokvarilo autentikaciju/deploy. Sutra treba: (a) pronaći gdje su te TOČNE vrijednosti izvorno spremljene (ako postoje zapisane negdje), ili (b) ako se regeneriraju, MORAJU se postaviti IDENTIČNO na OBA mjesta odjednom (Cloudflare Worker dashboard + GitHub secret) da autentikacija ostane usklađena.

**Provjereno programski koje secrets GitHub Actions stvarno koristi** — ovo je ISPRAVLJEN, konačan popis:

Stvarno potrebno za prijenos (5, ne 7):
- `CLOUDFLARE_ACCOUNT_ID`
- `CLOUDFLARE_API_TOKEN`
- `GNK_ASG_ADMIN_TOKEN`
- `GNK_ASG_OPERATOR_TOKEN`
- `OPERATOR_TOKEN`

**NE treba prenositi** (nisu GitHub Actions secrets, nego Cloudflare Worker runtime postavke, postavljene izravno na Cloudflare dashboardu za sam Worker, GitHub ih nikad ne čita):
- `ADMIN_TOKEN` — Worker runtime secret
- `OPERATOR_TOKEN_SHA256` — Worker runtime secret
- `OPENAI_API_KEY` — Worker runtime secret (postavljen danas)
- Ostali mail/API secrets korišteni unutar Workera — ostaju na Cloudflare strani, ne GitHubu

`MASARYKOVA_BACKUP_TOKEN`/`MASARYKOVA_MIRROR_TOKEN` se NE prenose — već postoje na odredištu (mi smo ih ranije postavili).

**Napomena:** vrijednosti postojećih secreta se NE MOGU pročitati (GitHub write-only politika) — vlasnik ima spremljeno 2-3 tokena (Cloudflare, GitHub), ostatak treba provjeriti/regenerirati.

### 2. Amazon Associates — ODUSTALO 27.7.2026. (vlasnikova odluka)
- **Neće se raditi.** Prijava je bila u tijeku (Store ID i opis sadržaja odgovoreni), ali vlasnik je odlučio odustati.
- Ako se predomisli u budućnosti, kontekst ostaje: put je bio kroz Creators API (stara PA-API više ne prima nove korisnike), trebalo je 3 kvalificirane prodaje za pun API pristup.
- HardwareSchotte ostaje zaključen (nema legitimnog puta bez njihove izravne dozvole) — Amazon je bio alternativa, sad ni ta nije aktivna.

### 3. Digital Workforce newsroom — pojedinačne stranice ✅ RIJEŠENO 27.7.2026.
- Napravljeno dok se čekalo rješavanje OpenAI ključa: `scripts/generate-digital-workforce-newsroom-pages-v1.mjs` generira pravu, indeksabilnu stranicu za svaki objavljeni newsroom članak (10 stranica: 5 HR + 5 EN), koristeći već postojeće ispravne canonical/SEO podatke iz Workera
- Automatizacija svakih 6h (`.github/workflows/generate-digital-workforce-newsroom-pages.yml`) za buduće članke (novi se objavljuje svaka 2 dana)
- Usput popravljen bug: newsroom kartice u tabu prije uopće nisu bile klikabilne (nema `<a href>`) — sad vode na prave stranice
- `editorial-content-deploy.yml` prošireno (i path trigger i sigurnosni whitelist regex) da pokriva nove putanje

### 4. HardwareSchotte
- Zaključeno: nema legitimnog puta bez njihove izravne dozvole/partnerstva
- Amazon je alternativni put u tijeku (vidi gore)

## Poznati manji nedostaci (nisu hitni, samo za evidenciju)
- `en/markets` i `en/intelligence-desk` dijele naslov s HR verzijom (vlastita imenica, ne prava greška)
- Trgovina nema još EN verziju (EN link trenutno vodi na istu HR stranicu)
- Jedna zaostala duplikat-stavka u news.json sa starim URL formatom (samoriješit će se u idućim ciklusima)

## NOVO — prijavljeno 27.7.2026., NE DIRATI bez izravnog dogovora (Mail Studio je zaštićeno područje)

**Problem 1 — jedno slanje evidentirano kao 3 zapisa:**
Na `https://gnk-asg.hr/mail-studio/` (email-status stranica, `GNK_ASG_EMAIL_STATUS_CLASSIC_FALLBACK_V1_20260710`), jedno slanje kontakt obrasca (stvarni primatelj `sinisa.vuckovic@mooveon.hr`, uz obavezni BCC `rht@gmx.com` i `beckuphome@gmail.com`) prikazuje se kao **3 odvojena "POSLANO" reda** (27.07.2026 09:28:35-38), umjesto jednog slanja s tri primatelja. Message ID-jevi su različiti za svaki red, sugerira da se svaki primatelj (uklj. BCC) evidentira kao zaseban "send" event.

**Problem 2 — statusi isporuke/otvaranja ne rade:**
Kolone "Isporučeno", "Prvo otvaranje", "Zadnje otvaranje" uvijek prazne (—) za sve zapise, "Otvaranja" uvijek 0. Nema vidljivog praćenja delivery/open eventova.

**Vlasnikova napomena:** "pamti ovo" — zapisati i riješiti sutra zajedno, ne dirati Mail Studio kod bez izravnog dogovora.

**Dodatan trag (bitno za dijagnozu sutra):** Vlasnik je pokazao `GNK ASG Webmail` sučelje (Inbox/Sent/Drafts/Outbox/Status/Health) koje za iste vrste slanja **ispravno prikazuje status isporuke** (npr. "1/1 delivered", "Još nije otvoreno") — dakle delivery tracking podaci **postoje** negdje u sustavu. Ovo sugerira da Mail Studio email-status stranica (Problem 2 gore) vjerojatno **ne dohvaća/ne povezuje** postojeće delivery podatke ispravno, umjesto da tracking uopće ne postoji. Vrijedi usporediti kako Webmail dohvaća status naspram kako to radi mail-studio/email-status stranica — mogući izvor iste podatkovne baze/KV-a, samo različit query/prikaz.

## RIJEŠENO 27.7.2026. — Mail Studio praćenje isporuke/otvaranja + SENT/INBOX popisi

Uz izravno dopuštenje vlasnika, sva tri gore navedena problema su istražena, pronađena i popravljena u tri odvojena, temeljito testirana koraka:

1. **Praćenje isporuke/otvaranja** (`outbound-mail-transport-v1.js`) — `sendBrandedEmail()` je slao preko sirovog Cloudflare `EmailMessage` objekta, zaobilazeći tracking sloj koji je očekivao strukturirani payload — pixel se nikad nije ugrađivao. Popravljeno: svaki primatelj sad dobiva vlastiti, jedinstveni tracking pixel/receipt link. **Potvrđeno na produkciji** — stvaran "OPENED" zapis s pravim IP-om/uređajem uhvaćen uživo.
2. **Webmail "SENT" popis** — otkriven treći, odvojen sustav (`mail_sync_messages` D1 tablica), koji se nikad automatski nije ažurirao nakon slanja kroz Mail Studio (samo ručni `backfillMailSync()`). Popravljeno: `studio()` sad odmah zove `recordMailSyncOutbound()` nakon uspješnog slanja.
3. **Webmail "INBOX" popis** — dolazna pošta se obrađuje u **potpuno odvojenom Cloudflare Workeru** (`gnk-asg-mail-center-worker`, Cloudflare Email Routing je šalje tamo, ne u `gnk-asg-direct-operator`). Taj worker je pisao samo u KV log (`mail:inbox`), nikad u `mail_sync_messages`. Popravljeno: dodana `recordMailSyncInbound()` funkcija, pozvana odmah nakon primitka. Potvrđeno: oba workera dijele **istu D1 bazu** (database_id `2116c4e5-1850-4888-91e7-c47deead3ced`), pa je popravak izravan (bez potrebe za međuworker komunikacijom). Isti `deploy-admin-auth-v6.yml` workflow već deploya oba workera.

### RIJEŠENO 27.7.2026. (dok je vlasnik bio odsutan) — mail-center-worker AI auto-reply tracking

Uklonjen stari, pokvareni `withEmailStatusTracking(env, 'direct-inbound-auto-reply')` omot u `gnk-asg-mail-center-worker/src/index.js`, isti obrazac kao popravak za contact-form ranije danas. `withBrandedMimeTransport` već poziva popravljeni `sendBrandedEmail()` za strukturirane pozive — vanjski omot je bio čisto suvišan/pokvaren duplikat. Dodana i `trackingSource:'direct-inbound-auto-reply'` oznaka za točan "Sustav" prikaz. Testirano lokalno (1 primatelj + 2 mandatorna BCC → 3 stvarna EmailMessage slanja, ispravno). Deployano.

## RIJEŠENO 27.7.2026. (poslijepodne) — ASG Bot / Intelligence Desk AI asistent

Vlasnik je pokazao da postoje **dva zasebna widgeta**:
1. **ASG Bot** (plutajući crveni krug, `floating-intelligence.js`/`.css`) — VEĆ spojen na pravi OpenAI backend (`/api/intelligence-desk-chat`, `gpt-4o-mini`), s ugrađenim sustavnim promptom o GNK ASG-u.
2. **"GNK ASG Intelligence Desk"** (inline forma na naslovnici, HR+EN) — bio **potpuno mrtav** (HTML forma postoji, nula JS logike bilo gdje na stranici).

Popravljeno:
- CSS kontrast na ASG Bot-u (gumbi tema i bot poruke imali tekst gotovo iste boje kao pozadina)
- Kontakt link `/kontakt/` → `/contact/` (prava, potpuna forma; `/kontakt/` nema ni `<form>` element)
- Proširen sustavni prompt sa stvarnim, javnim sadržajem sajta (Trgovina, AKTUAL MEDIA, Digital Workforce jasno označen kao simulacija, Projekti, itd.) — koristi oba widgeta
- Spojen "Intelligence Desk" inline forma na isti backend (novi `intelligence-desk-inline-chat-v1.js`)
- **KRITIČAN bug**: `app.js`'s `VERSION` cache-bust string se nije mijenjao od 23.7. — sve promjene na dinamički učitanim skriptama (uklj. gornje) ostajale su keširane kod posjetitelja. Popravljeno na svih 240 stranica (bump na `20260727-asg-bot-fixes-v1`).
- Donji red gumba (WhatsApp/Kontakt/Google News/Google) sad se centrira i prelama u redove umjesto da se odsijeca s desne strane panela

### RIJEŠENO 27.7.2026. (dok je vlasnik bio odsutan, 3h) — ASG Bot dodan na dodatne stranice

Vlasnik je izravno zatražio da ASG Bot (plutajući asistent) bude prisutan na "projekte, ideje, aktual media, i sve bitne stranice" jer bi mogao pomoći posjetiteljima. Provjereno:
- `/en/projects/` i `/gnk-aktual/` — **već** imaju app.js (vjerojatno riješeno ranijim cache popravkom, korisnik možda gledao prije osvježavanja)
- 5 stranica NIJE imalo `app.js` uopće: `synapse-demo`, `digital-solutions`, `ideje-u-djelovanju`, `nilus-bio`, `pulse-demo` — **dodano**, samo jedan `<script>` tag, ništa drugo dirano na tim stranicama

**Napomena:** ovih 5 stranica je na starijem "sticker-linked, ne diraj" popisu iz memorije — ali vlasnikov izravan, konkretan zahtjev danas nadjačava tu stariju napomenu za ovu specifičnu, minimalnu izmjenu (samo dodavanje widgeta, bez diranja sadržaja/dizajna/strukture tih stranica).

Nakon cache popravka, vlasnik javlja da pitanje "tko je direktor" i dalje ne dobiva pravi AI odgovor (pada na generički "ništa ne odgovara" fallback). Logika u `floating-intelligence.js` (`smartAnswer()`) je provjerena i ISPRAVNA — poziva pravi backend PRVO, keyword-fallback SAMO ako AI poziv pukne. Sumnja: `OPENAI_API_KEY` možda nije postavljen kao Cloudflare Worker secret (ne mogu provjeriti izravno, secrets su write-only).

**Dodan siguran, privremen dijagnostički alat**: `GET https://gnk-asg.hr/api/intelligence-desk-chat/diagnostic` — vraća SAMO `{openaiKeyConfigured: true/false, kvConfigured: true/false}`, nikad sam ključ. Deployano, ali NISAM ga mogao sam testirati (web_fetch alat ne dopušta dohvat proizvoljnog URL-a koji nije prethodno viđen u pretrazi).

**SUTRA/KAD SE VRATI VLASNIK**: posjetiti gornji URL u pregledniku, poslati mi točno što piše. Ako `openaiKeyConfigured: false` — treba postaviti `OPENAI_API_KEY` kao Cloudflare Worker secret na `gnk-asg-direct-operator` (vlasnik treba OpenAI API ključ, ako ga još nema treba ga generirati na platform.openai.com). Ako je `true`, treba dublje istražiti (možda pogrešan/istekao ključ, ili OpenAI rate/quota limit).
