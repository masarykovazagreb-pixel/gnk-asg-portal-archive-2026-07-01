# Public index visual fixes

Included in branch:
- black/gold public portal dashboard;
- logo boxes visible in header;
- THE CODE is shown on the index as a code/live-preview module, not as a small iframe;
- admin and protected modules are visible in a route map;
- long/raw links are not displayed as uncontrolled page text;
- 45 locations and 1537 workers are accessible through selectors/pages;
- favicon is already present in portal assets.
