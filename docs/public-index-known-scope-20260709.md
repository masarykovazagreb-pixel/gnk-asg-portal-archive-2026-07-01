# Known scope

This branch is public portal UI and static route-map preparation.

It does not run production deployment. It does not test authenticated runtime operations. It does not execute mail, contact form POST, campaign send, DNS, Cloudflare route, secret or token operations.
