# Deploy precheck — GNEW / GNK ASG Portal

This package is a local patch package, not a deployed change.

Required before deploy:
1. Add files to branch `fix/the-code-editorial-publications-20260708`.
2. Confirm Google verification file exists at `/google46686328e30c759f.html`.
3. Confirm `/the-code/` loads as full-screen cinematic HTML-video module.
4. Confirm THE CODE ends and stays on countdown screen.
5. Confirm finance KPI excludes ROI and ROA.
6. Confirm GNK ASG 2025 is labelled audited.
7. Confirm GNK DINAMO Ltd Group 2025 is labelled management-certified / internally reviewed / Colorado filing.
8. Confirm admin/backend pages remain protected.
9. Confirm no DNS, Cloudflare routes, secrets, production bindings, mail, campaign or scheduled outreach changes.
10. Deploy only through workflow `Deploy Admin Auth V6` with confirm input `DEPLOY_ADMIN_AUTH_V6`.
