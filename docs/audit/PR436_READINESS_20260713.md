# PR #436 readiness record — 2026-07-13

## Scope verified in branch

- Public navigation consolidated to one compact runtime.
- HR and EN Newsroom landing pages rebuilt.
- Published Newsroom articles separated from fallback source links.
- News queue guarded by operator authentication in the existing wrapper chain.
- Queue deduplication, URL validation and language routing added.
- Market renderers validate and escape external or generated data.
- Email signature accepts only validated email, HTTPS and CID values.
- No-send signature contract test added.
- Public route/link audit and Worker route ownership inventory added.
- Production verification helper records ownership headers and response preview.

## Explicitly not changed

- DNS.
- Cloudflare route assignments.
- Secrets and tokens.
- Existing production binding set.
- Campaign, bulk or scheduled outreach activation.
- Production deployment.
- Email delivery or mail tests.

## Current blocker

Production `/newsroom/` has returned a response identifying `gnk-asg-news-backend`. This repository contains neither source code nor Wrangler configuration for that Worker. The configured `gnk-asg-direct-operator` deployment is route-less and therefore cannot by itself resolve an external route ownership conflict.

## CI state

No commit status checks are attached to the current branch head. The audit workflow exists in the branch, but it has not executed. The PR must remain draft and must not be represented as tested or deploy-ready until the workflow is explicitly enabled and completes successfully.

## Required before review-ready

1. Run `Public Portal Audit` and inspect both JSON artifacts.
2. Confirm ownership and deployment source of `gnk-asg-news-backend`.
3. Decide route ownership separately from this code PR.
4. Re-review the final diff for infrastructure drift.
5. Keep production deployment behind `Deploy Admin Auth V6` and a new explicit confirmation.
