name: Refresh digital assets market

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    paths:
      - 'scripts/refresh_market.py'
      - '.github/workflows/refresh-market.yml'

permissions:
  contents: write

concurrency:
  group: refresh-digital-assets-market
  cancel-in-progress: true

jobs:
  refresh-market:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Refresh digital assets market data
        run: python scripts/refresh_market.py

      - name: Commit refreshed market data
        run: |
          git config user.name "gnk-asg-market-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/market.json data/fast_market_status.json
          if git diff --cached --quiet; then
            echo "No market data changes to commit."
          else
            git commit -m "Refresh digital assets market data"
            git push
          fi
