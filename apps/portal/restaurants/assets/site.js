const nav=document.getElementById('nav'); addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>28));
const menuBtn=document.getElementById('menuBtn'), links=document.getElementById('links');
menuBtn?.addEventListener('click',()=>links.classList.toggle('open')); links?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));
document.querySelectorAll('.location-switch>button').forEach(b=>b.addEventListener('click',()=>b.parentElement.classList.toggle('open')));
document.addEventListener('click',e=>{document.querySelectorAll('.location-switch.open').forEach(x=>{if(!x.contains(e.target))x.classList.remove('open')})});
document.querySelectorAll('.tab').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.dish').forEach(d=>d.hidden=!(f==='all'||d.dataset.cat===f))}));
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.1}); document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const date=document.getElementById('date'); if(date) date.min=new Date().toISOString().split('T')[0];
const translations=JSON.parse(document.getElementById('translations')?.textContent||'{}');
function setLang(lang){const dict=translations[lang];if(!dict)return;document.documentElement.lang=lang;document.body.dir=(lang==='ar'?'rtl':'ltr');document.querySelectorAll('[data-i18n]').forEach(el=>{const k=el.dataset.i18n;if(dict[k])el.textContent=dict[k]});document.querySelectorAll('.lang-btn').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));localStorage.setItem('gnkRoyalLang:'+location.pathname,lang)}
document.querySelectorAll('.lang-btn').forEach(b=>b.addEventListener('click',()=>setLang(b.dataset.lang))); setLang(localStorage.getItem('gnkRoyalLang:'+location.pathname)||document.body.dataset.defaultLang||'en');
document.getElementById('reservationForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget);const subject=encodeURIComponent(`GNK Royal ${document.body.dataset.city} reservation request`);const body=encodeURIComponent([...f.entries()].map(([k,v])=>`${k}: ${v}`).join('\n'));location.href=`mailto:${document.body.dataset.email}?subject=${subject}&body=${body}`;document.getElementById('formNotice').textContent='Your reservation request has been prepared in your email application. Our guest relations team will confirm availability.'});
