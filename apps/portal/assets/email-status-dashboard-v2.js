(()=>{
'use strict';
if(window.__GNK_ASG_EMAIL_STATUS_DASHBOARD_V2__)return;
window.__GNK_ASG_EMAIL_STATUS_DASHBOARD_V2__=true;
const params=new URLSearchParams(location.search),allowed=new Set(['all','mail-studio','campaign-mailer','media-center','auto-reply']);
const requested=params.get('source')||'all',source=allowed.has(requested)?requested:'all',query=(params.get('search')||'').slice(0,150),dateFilter=params.get('date')==='today'?'today':'all';
const candidate=params.get('from')||'',from=candidate.startsWith('/')&&!candidate.startsWith('//')?candidate:'';
const labels={'mail-studio':'Mail Studio','campaign-mailer':'Campaign Mailer','media-center':'Media Center','auto-reply':'Automatski odgovori',all:'svi sustavi'};
const statusLabels={SUBMITTING:'Priprema',ACCEPTED:'Prihvaćeno',DEFERRED:'Odgođeno',DELIVERED:'Dostavljeno',OPENED:'Otvoreno',BOUNCED:'Vraćeno',REJECTED:'Odbijeno',FAILED:'Neuspjelo',UNKNOWN:'Nepoznato'};
const nativeFetch=window.fetch.bind(window);
if(dateFilter==='today')window.fetch=(input,init)=>{try{if(typeof input==='string'||input instanceof URL){const url=new URL(String(input),location.origin);if(url.origin===location.origin&&url.pathname==='/api/email-status/records'){url.searchParams.set('date','today');input=url.pathname+url.search}}}catch{}return nativeFetch(input,init)};
const style=document.createElement('style');style.textContent='.gnk-status-help{margin:14px 0;padding:14px 16px;border:1px solid #cbd5e1;border-radius:14px;background:#f8fafc;color:#334155;font:500 13px/1.55 Arial}.gnk-status-help strong{color:#0f172a}.gnk-status-actions{display:flex;gap:9px;flex-wrap:wrap;margin:12px 0}.gnk-status-action{padding:10px 13px;border:1px solid #cbd5e1;border-radius:10px;background:#fff;color:#0f172a;font-weight:800;cursor:pointer;text-decoration:none}.gnk-status-action.primary{background:#0f172a;color:#fff;border-color:#0f172a}.gnk-status-note{margin-top:8px;color:#475569;font-size:12px}';document.head.append(style);
const translate=()=>document.querySelectorAll('td,th,option,button,span,strong,div').forEach(node=>{if(node.children.length)return;const text=(node.textContent||'').trim();if(statusLabels[text])node.textContent=statusLabels[text]});
const boot=()=>{document.documentElement.lang='hr';const select=document.querySelector('#source'),search=document.querySelector('#search'),load=document.querySelector('#load'),title=document.querySelector('h1'),top=document.querySelector('.top')||title?.parentElement;if(select)select.value=source;if(search&&query)search.value=query;const baseTitle=source==='all'?'Status svih email poruka':`Status poruka — ${labels[source]}`;if(title)title.textContent=dateFilter==='today'?`Današnje poruke — ${labels[source]}`:baseTitle;document.title=`${title?.textContent||'Status email poruka'} | GNK ASG`;
if(top&&!document.querySelector('.gnk-status-help')){const help=document.createElement('div');help.className='gnk-status-help';help.innerHTML='<strong>Kako čitati statuse:</strong> Prihvaćeno znači da je pružatelj preuzeo poruku. Dostavljeno znači da ju je prihvatio poslužitelj primatelja. Otvoreno je signal tracking slike i nije apsolutni dokaz čitanja.';const actions=document.createElement('div');actions.className='gnk-status-actions';const refresh=document.createElement('button');refresh.type='button';refresh.className='gnk-status-action primary';refresh.textContent='Osvježi podatke';refresh.onclick=()=>load?.click();actions.append(refresh);const toggle=document.createElement('a');toggle.className='gnk-status-action';const url=new URL(location.href);if(dateFilter==='today'){url.searchParams.delete('date');toggle.textContent='Prikaži sve datume'}else{url.searchParams.set('date','today');toggle.textContent='Samo današnje poruke'}toggle.href=url.pathname+url.search;actions.append(toggle);const sync=document.createElement('button');sync.type='button';sync.className='gnk-status-action';sync.textContent='Uskladi s Cloudflareom';const note=document.createElement('div');note.className='gnk-status-note';if(dateFilter==='today')note.textContent='Prikaz od 00:00 do 24:00 prema vremenskoj zoni Europe/Zagreb.';sync.onclick=async()=>{sync.disabled=true;note.textContent='Usklađivanje je u tijeku…';try{const response=await nativeFetch('/api/email-status/sync',{method:'POST',credentials:'same-origin',cache:'no-store'}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||data.error||`HTTP ${response.status}`);note.textContent=`Usklađivanje završeno. Obrađeno: ${data.events??data.eventCount??data.queried??0}.`;load?.click()}catch(error){note.textContent=`Usklađivanje nije završeno: ${error.message}`}finally{sync.disabled=false}};actions.append(sync);if(from){const back=document.createElement('a');back.className='gnk-status-action';back.href=from;back.textContent='← Natrag u aplikaciju';actions.append(back)}top.append(help,actions,note)}load?.click();translate();new MutationObserver(translate).observe(document.body,{subtree:true,childList:true})};
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();
})();
