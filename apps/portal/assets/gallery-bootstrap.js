// GNK ASG legacy gallery compatibility shim.
// The current portal no longer requires the removed gallery bootstrap implementation,
// but several public static pages still reference this stable asset URL.
// Keep this file side-effect free so those references remain valid while the
// canonical gallery/runtime layer is handled elsewhere.
(() => {
  'use strict';
})();
