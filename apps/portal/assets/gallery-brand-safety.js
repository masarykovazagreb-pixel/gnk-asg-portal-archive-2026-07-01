// GNK ASG legacy gallery brand-safety compatibility shim.
// Public static pages still reference this stable asset URL. The former
// implementation has been retired; this no-op shim prevents broken local
// references without introducing new runtime side effects or authority.
(() => {
  'use strict';
})();
