(()=>{
'use strict';
if(window.__GNK_MAIL_SYNC_CENTER_UI_V1__)return;window.__GNK_MAIL_SYNC_CENTER_UI_V1__=true;
const API='/api/mail-center/sync';
const state={folder:'inbox',profile:'',search:'',offset:0,limit:50,total:0,items:[],selected:null};
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const date=value=>{if(!value)return'—';try{return new Intl.DateTimeFormat('hr-HR',{dateStyle:'medium',timeStyle:'short',timeZone:'Europe/Zagreb'}).format(new Date(value));}catch{return value;}};
const names={office:'Office',legal:'Legal & Compliance',media:'Media Relations',press:'Press Office',it:'IT',assistant:'Executive Assistant',director:'Managing Director',sefic:'Executive Office',ubo:'UBO Office'};
async function api(path,options={}){const response=await fetch(API+path,{credentials:'same-origin',cache:'no-store',...options,headers:{accept:'application/json','content-type':'application/json',...(options.headers||{})}}),data=await response.json().catch(()=>({ok:false,error:`HTTP_${response.status}`}));if(!response.ok||!data.ok)throw new Error(data.message||data.error||`HTTP ${response.status}`);return data;}
function installStyle(){if(document.getElementById('gnkMailSyncStyle'))return;document.head.insertAdjacentHTML('beforeend',`<style id="gnkMailSyncStyle">
.mail-sync-tabs{display:flex;gap:7px;flex-wrap:wrap;padding:12px 20px;border-bottom:1px solid var(--line);background:rgba(255,255,255,.025)}.mail-sync-tab{border:1px solid var(--line);border-radius:999px;background:rgba(255,255,255,.06);color:#fff;padding:9px 12px;font:inherit;font-size:11px;font-weight:900;cursor:pointer}.mail-sync-tab.active{background:linear-gradient(135deg,#b98b2d,#f4d37a);color:#07101f;border-color:transparent}.mail-sync-panel{display:none;padding:18px}.mail-sync-panel.active{display:block}.mail-sync-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.mail-sync-head h2{margin:0;color:var(--gold)}.mail-sync-head p{margin:6px 0 0;color:var(--muted);font-size:12px}.mail-sync-actions{display:flex;gap:7px;flex-wrap:wrap}.mail-sync-button{border:1px solid var(--line);border-radius:999px;background:rgba(255,255,255,.07);color:#fff;padding:9px 12px;font-size:10px;font-weight:900;cursor:pointer}.mail-sync-button.gold{background:linear-gradient(135deg,#b98b2d,#f4d37a);color:#07101f}.mail-sync-filters{display:grid;grid-template-columns:1fr 220px auto;gap:9px;margin:14px 0}.mail-sync-filters input,.mail-sync-filters select{background:#f8fafc;color:#07101f}.mail-sync-layout{display:grid;grid-template-columns:minmax(380px,.85fr) minmax(0,1.15fr);gap:12px}.mail-sync-list,.mail-sync-detail{border:1px solid var(--line);border-radius:16px;background:rgba(255,255,255,.025);min-height:560px;overflow:hidden}.mail-sync-list{max-height:760px;overflow:auto}.mail-sync-row{width:100%;display:grid;grid-template-columns:10px 1fr auto;gap:10px;padding:12px;border:0;border-bottom:1px solid rgba(255,255,255,.07);border-radius:0;background:transparent;color:#fff;text-align:left;text-transform:none;letter-spacing:0}.mail-sync-row:hover,.mail-sync-row.active{background:rgba(212,175,55,.08)}.mail-sync-row.unread{background:rgba(96,165,250,.07)}.mail-sync-dot{width:8px;height:8px;margin-top:5px;border-radius:50%;background:#64748b}.mail-sync-row.unread .mail-sync-dot{background:#60a5fa}.mail-sync-copy{min-width:0}.mail-sync-copy strong,.mail-sync-copy span,.mail-sync-copy small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.mail-sync-copy span{margin-top:3px;color:#dbeafe}.mail-sync-copy small{margin-top:5px;color:var(--muted);font-weight:400}.mail-sync-time{color:var(--muted);font-size:9px;white-space:nowrap}.mail-sync-detail{padding:16px;overflow:auto}.mail-sync-empty{display:grid;place-items:center;min-height:420px;padding:30px;color:var(--muted);text-align:center}.mail-sync-meta{display:flex;gap:6px;flex-wrap:wrap;margin:10px 0}.mail-sync-pill{display:inline-flex;padding:5px 8px;border:1px solid rgba(255,255,255,.13);border-radius:999px;color:#cbd5e1;font-size:9px;font-weight:900;text-transform:uppercase}.mail-sync-body{margin-top:14px;padding:14px;border:1px solid rgba(255,255,255,.08);border-radius:13px;background:#fff;color:#111827;white-space:pre-wrap;line-height:1.55;overflow-wrap:anywhere}.mail-sync-frame{width:100%;min-height:420px;border:1px solid #d1d5db;border-radius:12px;background:#fff}.mail-sync-thread{display:grid;gap:9px;margin-top:14px}.mail-sync-thread-item{padding:10px;border-left:3px solid rgba(212,175,55,.55);background:rgba(255,255,255,.035)}.mail-sync-health{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin:14px 0}.mail-sync-stat{padding:12px;border:1px solid rgba(255,255,255,.08);border-radius:13px;background:rgba(255,255,255,.025)}.mail-sync-stat b{display:block;font-size:19px}.mail-sync-stat span{display:block;margin-top:4px;color:var(--muted);font-size:9px;text-transform:uppercase}.mail-sync-note{margin-top:12px;padding:11px;border:1px solid rgba(245,158,11,.35);border-radius:12px;color:#fde7b0;background:rgba(245,158,11,.07);font-size:11px;line-height:1.45}@media(max-width:1000px){.mail-sync-layout{grid-template-columns:1fr}.mail-sync-list{max-height:440px}.mail-sync-filters{grid-template-columns:1fr 1fr}.mail-sync-health{grid-template-columns:repeat(2,1fr)}}@media(max-width:620px){.mail-sync-panel{padding:12px}.mail-sync-head{display:block}.mail-sync-actions{margin-top:10px}.mail-sync-filters{grid-template-columns:1fr}.mail-sync-health{grid-template-columns:1fr}.mail-sync-row{grid-template-columns:9px 1fr}.mail-sync-time{grid-column:2}}
</style>`);}
function shell(){const studio=document.querySelector('.studio'),body=studio?.querySelector('.body'),head=studio?.querySelector('.head');if(!studio||!body||!head)return null;const tabs=document.createElement('nav');tabs.className='mail-sync-tabs';tabs.setAttribute('aria-label','Mail Studio views');tabs.innerHTML=[['compose','Compose'],['inbox','Inbox'],['sent','Sent'],['unread','Unread'],['sync','Sync Health']].map(([id,label])=>`<button class="mail-sync-tab${id==='compose'?' active':''}" type="button" data-mail-view="${id}">${label}</button>`).join('');head.insertAdjacentElement('afterend',tabs);const panel=document.createElement('section');panel.id='mailSyncPanel';panel.className='mail-sync-panel';panel.innerHTML=`<div class="mail-sync-head"><div><h2>Mail Sync Center</h2><p>Worker-native Inbox, Sent, threadovi i status sinkronizacije za devet GNK ASG sender profila.</p></div><div class="mail-sync-actions"><button class="mail-sync-button" id="mailSyncBackfill" type="button">Backfill audit</button><button class="mail-sync-button gold" id="mailSyncRefresh" type="button">Refresh</button></div></div><div class="mail-sync-filters" id="mailSyncFilters"><input id="mailSyncSearch" type="search" placeholder="Pretraži subject, adresu ili sadržaj"><select id="mailSyncProfile"><option value="">Svi profili</option>${Object.entries(names).map(([id,label])=>`<option value="${id}">${esc(label)}</option>`).join('')}</select><button class="mail-sync-button" id="mailSyncClear" type="button">Clear</button></div><div id="mailSyncHealth" hidden></div><div class="mail-sync-layout" id="mailSyncLayout"><div class="mail-sync-list" id="mailSyncList"></div><article class="mail-sync-detail" id="mailSyncDetail"><div class="mail-sync-empty">Odaberite poruku.</div></article></div><div class="mail-sync-note">Nova pošta može se automatski indeksirati tek nakon kontrolirane runtime aktivacije Workera. Povijesni audit se može uvesti bez vanjskog konektora; puni sadržaj stare GMX/Gmail/IMAP pošte zahtijevao bi zasebno odobren povijesni konektor.</div>`;body.insertAdjacentElement('afterend',panel);return{body,panel,tabs};}
function switchView(id,ui){document.querySelectorAll('[data-mail-view]').forEach(button=>button.classList.toggle('active',button.dataset.mailView===id));ui.body.hidden=id!=='compose';ui.panel.classList.toggle('active',id!=='compose');if(id==='compose')return;if(id==='sync'){showHealth();return;}document.getElementById('mailSyncHealth').hidden=true;document.getElementById('mailSyncLayout').hidden=false;document.getElementById('mailSyncFilters').hidden=false;state.folder=id;state.offset=0;load();}
function who(item){if(item.direction==='INBOUND')return item.fromName||item.fromEmail||'Unknown sender';return(item.to||[]).map(entry=>entry.name||entry.email).join(', ')||'Unknown recipient';}
function renderList(){const list=document.getElementById('mailSyncList');if(!state.items.length){list.innerHTML='<div class="mail-sync-empty">Nema poruka za odabrani pregled.</div>';return;}list.innerHTML=state.items.map(item=>`<button type="button" class="mail-sync-row${!item.isRead&&item.direction==='INBOUND'?' unread':''}${state.selected===item.id?' active':''}" data-message-id="${esc(item.id)}"><i class="mail-sync-dot"></i><span class="mail-sync-copy"><strong>${esc(who(item))}</strong><span>${esc(item.subject||'(no subject)')}</span><small>${esc(item.textPreview||item.processingStatus)}</small></span><time class="mail-sync-time">${esc(date(item.receivedAt||item.sentAt||item.createdAt))}</time></button>`).join('');list.querySelectorAll('[data-message-id]').forEach(button=>button.addEventListener('click',()=>openMessage(button.dataset.messageId)));}
async function load(){const params=new URLSearchParams({folder:state.folder,limit:String(state.limit),offset:String(state.offset)});if(state.profile)params.set('profile',state.profile);if(state.search)params.set('search',state.search);const list=document.getElementById('mailSyncList');list.innerHTML='<div class="mail-sync-empty">Učitavanje…</div>';try{const data=await api(`/messages?${params}`);state.items=data.items||[];state.total=data.total||0;renderList();}catch(error){list.innerHTML=`<div class="mail-sync-empty">${esc(error.message)}</div>`;}}
function recipients(items=[]){return items.map(item=>item.name?`${item.name} <${item.email}>`:item.email).join(', ')||'—';}
async function openMessage(id){state.selected=id;renderList();const detail=document.getElementById('mailSyncDetail');detail.innerHTML='<div class="mail-sync-empty">Učitavanje poruke…</div>';try{const data=await api(`/message?id=${encodeURIComponent(id)}`),m=data.message;detail.innerHTML=`<div class="mail-sync-head"><div><span class="mail-sync-pill">${esc(m.direction)}</span><h2 style="margin-top:9px">${esc(m.subject||'(no subject)')}</h2><p>${esc(m.fromName||m.fromEmail)} → ${esc(recipients(m.to))}</p></div><div class="mail-sync-actions">${m.direction==='INBOUND'?'<button class="mail-sync-button gold" id="mailSyncReply" type="button">Reply in Compose</button>':''}<button class="mail-sync-button" id="mailSyncToggleRead" type="button">${m.isRead?'Mark unread':'Mark read'}</button><button class="mail-sync-button" id="mailSyncArchive" type="button">Archive</button></div></div><div class="mail-sync-meta"><span class="mail-sync-pill">${esc(m.status)}</span><span class="mail-sync-pill">${esc(m.processingStatus)}</span><span class="mail-sync-pill">${esc(names[m.profileId]||m.profileId||'unassigned')}</span><span class="mail-sync-pill">${m.attachmentCount} attachments</span><span class="mail-sync-pill">${esc(date(m.receivedAt||m.sentAt||m.createdAt))}</span></div>${m.htmlBody?`<iframe id="mailSyncFrame" class="mail-sync-frame" sandbox="allow-popups allow-popups-to-escape-sandbox" title="Email content"></iframe>`:`<div class="mail-sync-body">${esc(m.textBody||'Sadržaj nije dostupan u povijesnom metadata zapisu.')}</div>`}<h3 style="color:var(--gold);margin-top:18px">Thread (${data.thread.length})</h3><div class="mail-sync-thread">${data.thread.map(item=>`<div class="mail-sync-thread-item"><strong>${esc(item.direction)} · ${esc(who(item))}</strong><br><small>${esc(date(item.receivedAt||item.sentAt||item.createdAt))} · ${esc(item.status)}</small><div style="margin-top:5px;color:var(--muted)">${esc(item.textPreview||item.processingStatus)}</div></div>`).join('')}</div>`;const frame=document.getElementById('mailSyncFrame');if(frame)frame.srcdoc=m.htmlBody;document.getElementById('mailSyncToggleRead')?.addEventListener('click',()=>changeState(m.id,m.isRead?'unread':'read'));document.getElementById('mailSyncArchive')?.addEventListener('click',()=>changeState(m.id,'archive'));document.getElementById('mailSyncReply')?.addEventListener('click',()=>reply(m));if(!m.isRead&&m.direction==='INBOUND')changeState(m.id,'read',false);}catch(error){detail.innerHTML=`<div class="mail-sync-empty">${esc(error.message)}</div>`;}}
async function changeState(id,action,reload=true){try{await api('/state',{method:'POST',body:JSON.stringify({id,action})});if(reload){state.selected=null;document.getElementById('mailSyncDetail').innerHTML='<div class="mail-sync-empty">Odaberite poruku.</div>';await load();}}catch(error){alert(error.message);}}
function reply(message){const to=document.getElementById('to'),subject=document.getElementById('subject'),body=document.getElementById('bodyText'),profile=document.getElementById('profile');if(to)to.value=message.fromEmail||'';if(subject)subject.value=/^re:/i.test(message.subject||'')?message.subject:`Re: ${message.subject||''}`;if(body)body.value=`\n\n--- Original message ---\nFrom: ${message.fromName||message.fromEmail}\nDate: ${date(message.receivedAt)}\nSubject: ${message.subject}\n\n${message.textBody||message.textPreview||''}`;if(profile&&message.profileId)profile.value=message.profileId;document.querySelector('[data-mail-view="compose"]')?.click();to?.focus();to?.dispatchEvent(new Event('input',{bubbles:true}));}
async function showHealth(){document.getElementById('mailSyncFilters').hidden=true;document.getElementById('mailSyncLayout').hidden=true;const host=document.getElementById('mailSyncHealth');host.hidden=false;host.innerHTML='<div class="mail-sync-empty">Učitavanje statusa…</div>';try{const data=await api('/health'),counts=data.counts||[],sum=direction=>counts.filter(item=>item.direction===direction).reduce((total,item)=>total+Number(item.count||0),0);host.innerHTML=`<div class="mail-sync-health"><div class="mail-sync-stat"><b>${sum('INBOUND')}</b><span>Inbox indexed</span></div><div class="mail-sync-stat"><b>${sum('OUTBOUND')}</b><span>Sent indexed</span></div><div class="mail-sync-stat"><b>${esc(data.mode)}</b><span>Sync mode</span></div><div class="mail-sync-stat"><b>${data.fullMailboxMirror?'YES':'NO'}</b><span>Full historical mirror</span></div></div><div class="mail-sync-body" style="background:rgba(255,255,255,.03);color:#fff"><strong>Last inbound:</strong> ${esc(date(data.state?.last_inbound_at))}\n<strong>Last outbound:</strong> ${esc(date(data.state?.last_outbound_at))}\n<strong>Last backfill:</strong> ${esc(date(data.state?.last_backfill_at))}\n<strong>Last error:</strong> ${esc(data.state?.last_error||'None')}\n\nFuture mail: ${esc(data.futureMail)}\nHistorical mail: ${esc(data.historicalMail)}</div>`;}catch(error){host.innerHTML=`<div class="mail-sync-empty">${esc(error.message)}</div>`;}}
async function backfill(){const button=document.getElementById('mailSyncBackfill');button.disabled=true;try{const data=await api('/backfill',{method:'POST',body:'{}'});alert(`Backfill completed. Inbox: ${data.stored?.inbound||0}, Sent: ${data.stored?.outbound||0}`);if(state.folder==='sync')showHealth();else load();}catch(error){alert(error.message);}finally{button.disabled=false;}}
function install(){installStyle();const ui=shell();if(!ui)return;ui.tabs.querySelectorAll('[data-mail-view]').forEach(button=>button.addEventListener('click',()=>switchView(button.dataset.mailView,ui)));let timer=0;document.getElementById('mailSyncSearch').addEventListener('input',event=>{clearTimeout(timer);timer=setTimeout(()=>{state.search=event.target.value.trim();state.offset=0;load();},300);});document.getElementById('mailSyncProfile').addEventListener('change',event=>{state.profile=event.target.value;state.offset=0;load();});document.getElementById('mailSyncClear').addEventListener('click',()=>{state.search='';state.profile='';document.getElementById('mailSyncSearch').value='';document.getElementById('mailSyncProfile').value='';load();});document.getElementById('mailSyncRefresh').addEventListener('click',()=>state.folder==='sync'?showHealth():load());document.getElementById('mailSyncBackfill').addEventListener('click',backfill);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();
