(()=>{if(window.__GNK_BUSINESS_NEWS__)return;window.__GNK_BUSINESS_NEWS__=true;const root=document.getElementById('newsGrid'),status=document.getElementById('newsStatus'),actions=document.querySelector('.news-actions');if(!root||!status)return;const en=document.body.dataset.language==='en',locale=en?'en-GB':'hr-HR';const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const labels={loading:en?'Loading business news…':'Učitavanje poslovnih vijesti…',open:en?'Open article or source':'Otvori članak ili izvor',none:en?'News feed is temporarily unavailable.':'Vijesti trenutačno nisu dostupne.',refresh:en?'Refresh news':'Osvježi vijesti',refreshing:en?'Refreshing…':'Osvježavanje…',updated:en?'Last refresh':'Zadnje osvježavanje',items:en?'items':'vijesti'};function ts(v){const n=Date.parse(v||'');return Number.isFinite(n)?n:0}function fmt(n){return n?new Intl.DateTimeFormat(locale,{timeZone:'Europe/Zagreb',dateStyle:'medium',timeStyle:'short'}).format(new Date(n)):(en?'unknown':'nepoznato')}function valid(v){try{const u=new URL(String(v||''),location.origin);return['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}}function normalize(payload){const raw=Array.isArray(payload)?payload:(Array.isArray(payload?.items)?payload.items:[]),seen=new Set(),items=[];for(const item of raw){const url=valid(item?.url||item?.sourceUrl),title=String(item?.title||'').trim();if(!url||!title)continue;const key=String(item?.id||url).toLowerCase();if(seen.has(key))continue;seen.add(key);items.push({...item,url,title,summary:String(item?.summary||item?.description||'').trim(),source:String(item?.source||item?.region||item?.category||'GNK ASG'),publishedAt:item?.publishedAt||item?.published_at||''})}items.sort((a,b)=>ts(b.publishedAt)-ts(a.publishedAt));const newest=Math.max(ts(payload?.updatedAt||payload?.generatedAt),...items.map(i=>ts(i.publishedAt)),0);return{items,newest,status:String(payload?.status||'').toLowerCase()}}function state(feed){if(feed.status==='live'&&Date.now()-feed.newest<10800000)return'live';if(Date.now()-feed.newest<64800000)return'snapshot';if(Date.now()-feed.newest<259200000)return'delayed';return'fallback'}function card(item){const meta=[item.source,fmt(ts(item.publishedAt))].filter(Boolean).map(esc).join(' · ');return`<article class="news-card">${item.image?`<img src="${esc(valid(item.image))}" alt="${esc(item.imageAlt||item.title)}" loading="lazy">`:''}<small>${meta}</small><h2>${esc(item.title)}</h2>${item.summary?`<p>${esc(item.summary)}</p>`:''}<a href="${esc(item.url)}" target="${item.url.startsWith(location.origin)?'_self':'_blank'}" rel="noopener noreferrer">${labels.open} →</a></article>`}async function load(){status.textContent=labels.loading;try{const r=await fetch(`/data/news.json?cb=${Date.now()}`,{cache:'no-store',headers:{accept:'application/json'}});if(!r.ok)throw new Error(`HTTP ${r.status}`);const feed=normalize(await r.json());if(!feed.items.length)throw new Error('empty_news_feed');root.innerHTML=feed.items.slice(0,48).map(card).join('');const s=state(feed);status.dataset.status=s;status.innerHTML=`<strong>${s.toUpperCase()}</strong> · ${labels.updated}: ${esc(fmt(feed.newest))} · ${feed.items.length} ${labels.items}`}catch(e){root.innerHTML=`<article class="news-card"><h2>${labels.none}</h2><p>${esc(e.message)}</p></article>`;status.dataset.status='fallback';status.innerHTML=`<strong>FALLBACK</strong> · ${labels.none}`}}async function refresh(button){button.disabled=true;button.textContent=labels.refreshing;try{const r=await fetch('/api/news-refresh',{method:'POST',cache:'no-store',headers:{accept:'application/json'}});const data=await r.json().catch(()=>({}));if(!r.ok&&r.status!==429)throw new Error(data.error||`HTTP ${r.status}`);await load()}catch(e){status.textContent=e.message}finally{button.disabled=false;button.textContent=labels.refresh}}if(actions&&!document.getElementById('newsRefreshButton')){const b=document.createElement('button');b.id='newsRefreshButton';b.type='button';b.textContent=labels.refresh;b.addEventListener('click',()=>refresh(b));actions.appendChild(b)}load()})();