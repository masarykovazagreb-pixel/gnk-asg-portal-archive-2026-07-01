import fs from 'node:fs';
import path from 'node:path';
const ROOT=process.cwd(),PORTAL=path.join(ROOT,'apps','portal'),failures=[];const stats={pages:0,checked:0,violations:0};
const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name);return e.isDirectory()&&!['assets','data'].includes(e.name)?walk(p):e.isFile()&&e.name==='index.html'?[p]:[]});
const strip=s=>s.replace(/<[^>]+>/g,' ').replace(/&nbsp;/gi,' ').replace(/\s+/g,' ').trim();
for(const file of walk(PORTAL)){stats.pages++;const html=fs.readFileSync(file,'utf8');if(/<meta\b[^>]*(?:name=["']robots["'][^>]*content=["'][^"']*noindex|content=["'][^"']*noindex[^"']*["'][^>]*name=["']robots["'])/i.test(html))continue;stats.checked++;const rel='/'+path.relative(PORTAL,file).replaceAll(path.sep,'/');const titles=[...html.matchAll(/<title\b[^>]*>([\s\S]*?)<\/title>/gi)].map(m=>strip(m[1])).filter(Boolean);const h1=[...html.matchAll(/<h1\b[^>]*>([\s\S]*?)<\/h1>/gi)].map(m=>strip(m[1])).filter(Boolean);if(titles.length!==1){failures.push(`${rel}: indexable page must have exactly one non-empty title element (found ${titles.length})`);stats.violations++;}if(h1.length!==1){failures.push(`${rel}: indexable page must have exactly one non-empty H1 (found ${h1.length})`);stats.violations++;}}
const report={version:'GNK_ASG_INDEXABLE_HEADING_CONTRACT_V1',ok:failures.length===0,stats,failures};const out=path.join(ROOT,'artifacts','indexable-heading-contract');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'report.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(failures.length)process.exit(1);
