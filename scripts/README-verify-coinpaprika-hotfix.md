# CoinPaprika verifier hotfix

The production market runtime already accepts `coinpaprika-tickers` as a live upstream. The V38 production verifier must accept the same upstream while continuing to reject static fallback responses.
