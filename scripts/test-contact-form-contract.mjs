import fs from 'node:fs';
import assert from 'node:assert/strict';

const hr=fs.readFileSync('apps/portal/contact/index.html','utf8');
const en=fs.readFileSync('apps/portal/en/contact/index.html','utf8');
const client=fs.readFileSync('apps/portal/assets/contact-form-v2.js','utf8');
const handler=fs.readFileSync('workers/gnk-asg-direct-operator/src/contact-studio-mail-v1.js','utf8');
const transport=fs.readFileSync('workers/gnk-asg-direct-operator/src/outbound-mail-transport-v1.js','utf8');
const worker=fs.readFileSync('workers/gnk-asg-direct-operator/src/index-unified-auth-v21.js','utf8');
const signature=fs.readFileSync('workers/gnk-asg-direct-operator/src/email-signature-contract-v1.js','utf8');

for(const page of [hr,en]){
 assert.match(page,/id="contactForm"/);
 assert.match(page,/name="department"/);
 assert.match(page,/name="email" type="email"/);
 assert.match(page,/name="subject"/);
 assert.match(page,/name="message"/);
 assert.match(page,/name="consent"/);
 assert.match(page,/name="website"/);
 assert.match(page,/contact-form-v2\.js/);
 assert.match(page,/logo-gnk-asg-canonical\.svg/);
}
assert.match(client,/fetch\('\/api\/contact-submit'/);
assert.match(client,/content-type':'application\/json/);
assert.match(client,/credentials:'same-origin'/);
assert.match(client,/deliveryOk/);
assert.match(client,/if\(!response\.ok\)throw/);
assert.match(client,/if\(!out\.stored\)throw/);
assert.match(client,/out\.accepted&&out\.stored/);
assert.doesNotMatch(client,/!response\.ok&&!out\.accepted/);
assert.doesNotMatch(client,/alert\(/);

assert.match(handler,/CONTACT_PATH='\/api\/contact-submit'/);
assert.match(handler,/parseContact/);
assert.match(handler,/application\/json/);
assert.match(handler,/multipart\/form-data/);
assert.match(handler,/createContactCase/);
assert.match(handler,/generateCaseId/);
assert.match(handler,/sendBrandedEmail/);
assert.match(handler,/internalMail/);
assert.match(handler,/autoReply/);
assert.match(handler,/deliveryOk/);
assert.match(handler,/CONTACT_INTERNAL='rht@gmx\.com'/);
assert.match(handler,/attachments:parsed\.attachments/);
assert.match(handler,/CONTACT_RATE_WINDOW_SECONDS=900/);
assert.match(handler,/CONTACT_RATE_LIMIT=5/);
assert.match(handler,/enforceContactRateLimit/);
assert.match(handler,/contact:rate:/);
assert.match(handler,/cf-connecting-ip/);
assert.match(handler,/expirationTtl:CONTACT_RATE_WINDOW_SECONDS\+60/);
assert.match(handler,/retry-after/);
assert.match(handler,/MAX_ATTACHMENTS=3/);
assert.match(handler,/MAX_ATTACHMENT_BYTES=3_200_000/);
assert.match(handler,/MAX_TOTAL_ATTACHMENT_BYTES=6_400_000/);
assert.match(handler,/too_many_attachments/);
assert.match(handler,/attachments_total_too_large/);
assert.match(handler,/request_too_large/);
assert.match(handler,/CONTACT_JSON_MAX_BYTES=32_000/);
assert.match(handler,/CONTACT_ORIGINS=new Set\(\['https:\/\/gnk-asg\.hr','https:\/\/www\.gnk-asg\.hr'\]\)/);
assert.match(handler,/contactOriginAllowed\(request\)/);
assert.match(handler,/origin_not_allowed/);
assert.match(handler,/const raw=await request\.text\(\)/);
assert.match(handler,/new TextEncoder\(\)\.encode\(raw\)\.byteLength>CONTACT_JSON_MAX_BYTES/);
assert.match(handler,/email=clean\(body\.email,200\)/);
assert.match(handler,/subject=clean\(body\.subject,220\)/);
assert.match(handler,/message=clean\(body\.message,8000\)/);
const storageGate=handler.indexOf('if(!stored){');
const firstMailAttempt=handler.indexOf('let internalMail=null');
assert.ok(storageGate>=0&&firstMailAttempt>=0&&storageGate<firstMailAttempt,'D1 storage must succeed before any mail attempt');
assert.match(handler,/accepted:false,stored:false,mailAttempted:false/);
assert.match(handler,/contact_storage_unavailable/);
assert.match(handler,/delivery:\{internal:internalSent,acknowledgement:acknowledgementSent\}/);
assert.doesNotMatch(handler,/return json\(\{[^}]*internalMail,autoReply/);
assert.doesNotMatch(handler,/will respond after review|odgovorit će nakon obrade/);
assert.match(handler,/async function authorised\(request,env,ctx,app\)/);
assert.match(handler,/new URL\('\/api\/operator-auth-check',request\.url\)/);
assert.match(handler,/if\(!response\.ok\)return response/);
assert.match(handler,/payload\?\.authenticated===true\?null/);
assert.match(handler,/auth_probe_failed/);
assert.match(handler,/auth_probe_invalid/);
assert.match(handler,/www-authenticate':'Session'/);
assert.match(handler,/if\(!sameOrigin\(request\)\)return json\(\{ok:false,error:'origin_not_allowed'\},403\)/);
assert.match(handler,/method_not_allowed'\},405,\{'allow':'POST'\}/);
assert.doesNotMatch(handler,/\/api\/mail-sync\/health/);
assert.doesNotMatch(handler,/return response\.ok\?null:response/);
assert.match(handler,/const complete=result\.ok===true&&result\.failed===0/);
assert.match(handler,/complete\?200:207/);
assert.match(handler,/failures:result\.failures\|\|\[\]/);
assert.doesNotMatch(handler,/return json\(\{ok:true,status:result\.status/);
assert.match(handler,/error:'mail_transport_failed'/);
assert.doesNotMatch(handler,/env\.EMAIL\.send\(enforceRequiredSignature/);

const studioStart=handler.indexOf('async function studio(');
assert.ok(studioStart>=0,'Studio handler must exist');
const studioSource=handler.slice(studioStart);
assert.doesNotMatch(studioSource,/error:clean\(error\?\.message\|\|error/);
const originGate=studioSource.indexOf("if(!sameOrigin(request))");
const authGate=studioSource.indexOf('const denied=await authorised(request,env,ctx,app)',originGate);
const sendCall=studioSource.indexOf('const result=await sendBrandedEmail',authGate);
assert.ok(originGate>=0&&authGate>=0&&sendCall>=0&&originGate<authGate&&authGate<sendCall,'Studio send must enforce origin and explicit session before transport');

assert.match(transport,/import \{EmailMessage\} from 'cloudflare:email'/);
assert.match(transport,/new EmailMessage\(prepared\.from,recipient,prepared\.raw\)/);
assert.match(transport,/enforceRequiredSignature/);
assert.match(transport,/safeHeader=value=>clean\(value\)\.replace\(\/\[\\r\\n\]\+\/g,' '\)/);
assert.match(transport,/Content-ID:/);
assert.match(transport,/Content-Location:/);
assert.match(transport,/X-Attachment-Id:/);

assert.match(worker,/handlesContactStudio\(path\)/);
assert.match(worker,/handleContactStudio\(request,env,ctx,app\)/);
assert.doesNotMatch(worker,/CLOUDFLARE_API_TOKEN|GNK_ASG_OPERATOR_TOKEN/);
assert.match(signature,/logo-gnk-asg-email\.png/);
assert.match(signature,/canonical-png-64x66/);
console.log(JSON.stringify({ok:true,forms:['hr','en'],endpoint:'/api/portal-contact-submit',payloads:['json','multipart'],storage:'D1',mailTransport:'Cloudflare EmailMessage',mail:['internal','acknowledgement'],rateLimit:{requests:5,windowSeconds:900,store:'existing KV',failClosed:true},origin:{allowed:['https://gnk-asg.hr','https://www.gnk-asg.hr'],failClosed:true},bodyLimit:{jsonBytes:32000},storageBeforeMail:true,publicDeliveryDetailsRedacted:true,attachments:{count:3,perFileBytes:3200000,totalBytes:6400000,pdfOnly:true},studioAuth:{probe:'/api/operator-auth-check',requiresAuthenticatedTrue:true,failClosedOn:['cross-origin','401','403','404','5xx','exception','malformed-json']},studioDelivery:{completeStatus:200,partialStatus:207,partialOk:false,failuresExposed:true,transportErrorsRedacted:true},inlineLogo:true,mailSent:false},null,2));
