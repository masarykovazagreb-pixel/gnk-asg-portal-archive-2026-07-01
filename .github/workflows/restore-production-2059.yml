name: Archived legacy workflow - restore-production-2059.yml

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived. Production deployment is permitted only through deploy-admin-auth-v6.yml with DEPLOY_ADMIN_AUTH_V6."
