name: Check live V28 (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy check notice
        run: echo "V28 live checks are archived; current production checks run in Deploy GNK ASG Portal."
