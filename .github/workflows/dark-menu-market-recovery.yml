name: Production Index and Content Automation (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "This legacy dark-market/UI-harmonizer deployment is archived."
          echo "Production is deployed only by Deploy GNK ASG Portal."
