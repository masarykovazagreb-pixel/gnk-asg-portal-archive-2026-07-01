name: Index Polish V30 Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-index-polish-v30
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo manual
