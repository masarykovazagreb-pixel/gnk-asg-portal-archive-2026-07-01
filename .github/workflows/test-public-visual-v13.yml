name: Test Public Visual V13 (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy visual test notice
        run: echo "V13 visual checks are archived; current portal validation runs in Deploy GNK ASG Portal."
