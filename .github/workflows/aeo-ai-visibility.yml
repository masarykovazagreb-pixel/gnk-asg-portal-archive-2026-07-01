name: AEO / AI Visibility Boost

# Generira /llms.txt, /llms-full.txt, entity JSON-LD graf, /ai/ endpoint;
# ažurira robots.txt s eksplicitnim dozvolama za AI crawlere; commit + exact-SHA deploy.

on:
  workflow_dispatch:
  schedule:
    - cron: '30 3 * * *'   # svaki dan u 03:30 UTC (05:30 Zagreb) — nakon noćnog SEO audita

permissions:
  contents: write
  actions: write

concurrency:
  group: aeo-ai-visibility
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Generate AEO artefacts
        run: node scripts/generate-aeo-artifacts-v1.mjs
      - name: Enrich image alt/title with entities (full name)
        run: node scripts/image-entity-seo-v1.mjs
      - name: Boost hashtags in article bodies
        run: node scripts/hashtag-boost-v1.mjs
      - name: Editorial entity linkage (JSON-LD Article + og:image fallback)
        run: node scripts/editorial-entity-linkage-v1.mjs
      - name: Share buttons on editorial pages
        run: node scripts/add-share-buttons-v1.mjs
      - name: Rich meta-descriptions from real article body
        run: node scripts/rich-descriptions-v1.mjs
      - name: Site-wide SEO guardian (non-editorial pages)
        run: node scripts/site-seo-guardian-v1.mjs
      - name: BreadcrumbList JSON-LD
        run: node scripts/add-breadcrumbs-v1.mjs
      - name: Generate RSS + Atom feeds
        run: node scripts/generate-feeds-v1.mjs
      - name: Feed discovery link tags
        run: node scripts/add-feed-discovery-v1.mjs
      - name: Regenerate image sitemap
        run: python3 scripts/generate-image-sitemap.py
      - name: Commit + dispatch exact-SHA production deploy
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "gnk-aeo-bot"
          git config user.email "actions@github.com"
          git add apps/portal
          if git diff --cached --quiet; then
            echo "Nema promjena u AEO artefaktima."
            exit 0
          fi
          git commit -m "AEO+Entity: refresh /llms.txt, /llms-full.txt, entity graph, /ai/, image alt/title, hashtags, editorial JSON-LD, sitemap"
          git push origin HEAD:main
          sha="$(git rev-parse HEAD)"
          gh workflow run deploy-admin-auth-v6.yml \
            --repo beckuphome-gnk/gnk-asg-portal \
            --ref main \
            -f confirm_production_deploy=DEPLOY_ADMIN_AUTH_V6 \
            -f approved_sha="$sha"
