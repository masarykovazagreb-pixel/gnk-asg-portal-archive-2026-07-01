name: Strengthen Objave SEO V2

on:
  push:
    branches:
      - agent/strengthen-objave-seo-v2-20260806
    paths:
      - 'scripts/strengthen-objave-seo-v2.mjs'
      - '.github/workflows/strengthen-objave-seo-v2.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: strengthen-objave-seo-v2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/strengthen-objave-seo-v2-20260806
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply stronger metadata and schema
        run: node scripts/strengthen-objave-seo-v2.mjs
      - name: Verify generated SEO contract
        run: node scripts/strengthen-objave-seo-v2.mjs --check
      - name: Commit generated page
        run: |
          set -euo pipefail
          if git diff --quiet -- apps/portal/objave/index.html; then
            echo 'No generated changes.'
            exit 0
          fi
          git config user.name 'gnk-asg-seo-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add apps/portal/objave/index.html
          git commit -m 'feat(seo): strengthen objave archive metadata and schema v2 [skip ci]'
          git push origin HEAD:agent/strengthen-objave-seo-v2-20260806
