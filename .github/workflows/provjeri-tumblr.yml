name: Provjeri Tumblr verifikaciju (jednokratno)
on: workflow_dispatch
jobs:
  provjeri:
    runs-on: ubuntu-latest
    permissions: {contents: write}
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p apps/portal/data/tmp-provjera
          curl -s "https://nermin-sefic.tumblr.com/" -o apps/portal/data/tmp-provjera/stranica.html
          grep -o 'google-site-verification[^>]*' apps/portal/data/tmp-provjera/stranica.html > apps/portal/data/tmp-provjera/rezultat.txt || echo "NIJE NADJENO" > apps/portal/data/tmp-provjera/rezultat.txt
          cat apps/portal/data/tmp-provjera/rezultat.txt
      - run: |
          git config user.name "gnk-asg-bot"; git config user.email "it@gnk-asg.hr"
          BRANCH="provjera-tumblr-${{ github.run_id }}"
          git checkout -b "$BRANCH"
          git add apps/portal/data/tmp-provjera/rezultat.txt
          git commit -m "provjera"
          git push origin "$BRANCH"
