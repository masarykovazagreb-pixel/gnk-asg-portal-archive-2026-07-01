name: Diagnose Production Candidate a82af2a

on:
  pull_request:

permissions:
  contents: read

jobs:
  export-contrast-audit-source:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/editorial-paragraph-contract-20260724
      - name: Upload exact contrast audit source
        uses: actions/upload-artifact@v4
        with:
          name: contrast-audit-source-pr754
          path: apps/portal/tests/all-pages-visual-contrast.spec.js
          retention-days: 7
