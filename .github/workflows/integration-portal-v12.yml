name: Integration Portal V12 (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy integration test notice
        run: echo "V12 integration checks are archived; current portal validation runs in Deploy GNK ASG Portal."
