import app,{VERSION as BASE_VERSION} from './index-unified-auth-v17.js';
import {EmailMessage} from 'cloudflare:email';
import {renderBrandSignatureHtml,renderBrandSignatureText,LOGO_URL,VERSION as SIGNATURE_VERSION} from './email-brand-signature-v1.js';
import {loadEmailLogo,EMAIL_LOGO_CID,foldBase64,VERSION as MIME_VERSION} from './email-brand-mime-v1.js';

export const VERSION=`GNK_ASG_UNIFIED_AUTH_V66_20260713_CONTROLLED_MAIL_RECIPIENTS_${BASE_VERSION}`;
const SEND_PATHS=new Set(['/api/studio-message/send','/api/admin-mail-send']);
const SCHEDULE_PREFIX='/api/mail-schedule';
const DEFAULT_MANDATORY_BCC=['beckuphome@gmail.com'];
const ALLOWED_FROM_DOMAIN='gnk-asg.hr';
const MAX_RECIPIENTS=25;
const PUBLIC_HTML_ROUTES=new Map([['/newsroom','/newsroom/index.html'],['/en/newsroom','/en/newsroom/index.html']]);
const PROFILE_UNITS={office:'Office',legal:'Legal & Compliance',media:'Media Relations & Accreditation Center',it:'IT / Digital Operations',director:'Managing Director / Authorised Representative'};
const RELEASE_SCRIPT='<script defer src="/assets/release-completion-v1.js?v=20260720-fix2"></script>';
const clean=value=>String(value??'').trim();
const pathOf=request=>new URL(request.url).pathname.replace(/\/+$/,'')||'/';
const json=(data,status=200)=>new Response(JSON.stringify(data,null,2),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-gnk-mail-transport':VERSION}});
const parseEmails=value=>[...new Set(String(value||'').split(/[;,\s]+/).map(v=>clean(v).toLowerCase()).filter(v=>/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/i.test(v)))];
const safeHeader=value=>clean(value).replace(/[\r\n]+/g,' ');
const allowedFrom=value=>{const email=parseEmails(value)[0]||'';return email.endsWith(`@${ALLOWED_FROM_DOMAIN}`)?email:''};
const mandatoryBcc=env=>{const configured=parseEmails(env.MAIL_MANDATORY_BCC);return configured.length?configured:DEFAULT_MANDATORY_BCC};
const bytesToBase64=bytes=>{let out='';const value=bytes instanceof Uint8Array?bytes:new Uint8Array(bytes);for(let i=0;i<value.length;i+=32768)out+=String.fromCharCode(...value.subarray(i,Math.min(i+32768,value.length)));return foldBase64(btoa(out));};
const textBase64=value=>bytesToBase64(new TextEncoder().encode(String(value||'')));
async function authenticated(request,env,ctx){const target=new URL('/api/operator-auth-check',request.url),check=new Request(target,{method:'GET',headers:request.headers,redirect:'manual'});return (await app.fetch(check,env,ctx)).ok;}
function brandedContent(body,from,fromName,logoSrc){const profile=clean(body.profile||body.signatureProfile||'office').toLowerCase(),unit=PROFILE_UNITS[profile]||'Corporate Communications',signatureArgs={marker:`${VERSION}_${SIGNATURE_VERSION}`,name:fromName||'GNK ASG',unit,subline:'GNK ASG d.o.o.',address:'Zagrebačka cesta 130, 10090 Zagreb',registry:'OIB: 75227917632 · MBS: 081512375',email:from,web:'https://gnk-asg.hr',logoSrc},baseText=clean(body.text||body.plainText||body.body||''),baseHtml=clean(body.html||body.bodyHtml||body.body||'')||`<div style="font-family:Arial,Helvetica,sans-serif;color:#111827;font-size:15px;line-height:1.6">${baseText.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c])).replace(/\n/g,'<br>')}</div>`;return{text:`${baseText}${baseText?'\n\n':''}${renderBrandSignatureText(signatureArgs)}`,html:`${baseHtml}${renderBrandSignatureHtml(signatureArgs)}`};}
async function rawMessage(env,{from,fromName,to,subject,body}){const logo=await loadEmailLogo(env),logoSrc=logo?`cid:${EMAIL_LOGO_CID}`:LOGO_URL,content=brandedContent(body,from,fromName,logoSrc),alt=`gnk_alt_${crypto.randomUUID().replace(/-/g,'')}`,rel=`gnk_rel_${crypto.randomUUID().replace(/-/g,'')}`,headers=[`From: ${safeHeader(fromName||'GNK ASG')} <${safeHeader(from)}>` ,`To: ${safeHeader(to)}`,`Reply-To: ${safeHeader(from)}`,`Subject: ${safeHeader(subject)}`,`Date: ${new Date().toUTCString()}`,`Message-ID: <${crypto.randomUUID()}@gnk-asg.hr>`,'MIME-Version: 1.0',`X-GNK-ASG-Mail-Transport: ${VERSION}`,`X-GNK-ASG-Signature-Version: ${SIGNATURE_VERSION}`,`X-GNK-ASG-Mime-Version: ${MIME_VERSION}`,`X-GNK-ASG-Signature-Logo: ${logo?'cid-inline':'remote-fallback'}`],alternative=[`--${alt}`,'Content-Type: text/plain; charset=UTF-8','Content-Transfer-Encoding: base64','',textBase64(content.text),`--${alt}`,'Content-Type: text/html; charset=UTF-8','Content-Transfer-Encoding: base64','',textBase64(content.html),`--${alt}--`].join('\r\n');if(!logo)return`${headers.join('\r\n')}\r\nContent-Type: multipart/alternative; boundary="${alt}"\r\n\r\n${alternative}\r\n`;const related=[`Content-Type: multipart/related; boundary="${rel}"`,'',`--${rel}`,`Content-Type: multipart/alternative; boundary="${alt}"`,'',alternative,`--${rel}`,`Content-Type: ${logo.contentType}; name="${logo.filename}"`,`Content-Disposition: inline; filename="${logo.filename}"`,`Content-ID: <${EMAIL_LOGO_CID}>`,'Content-Transfer-Encoding: base64','',bytesToBase64(logo.bytes),`--${rel}--`].join('\r\n');return`${headers.join('\r\n')}\r\n${related}\r\n`;}
async function sendRaw(request,env,ctx){if(!await authenticated(request,env,ctx))return json({ok:false,error:'unauthorized',message:'Operator/admin session required.'},401);if(!env.EMAIL?.send)return json({ok:false,error:'email_binding_missing',message:'Cloudflare EMAIL binding is not configured.'},503);let body;try{body=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const from=allowedFrom(body.from?.email||body.from||'office@gnk-asg.hr'),fromName=clean(body.fromName||body.from?.name||'GNK ASG'),to=parseEmails(body.to),cc=parseEmails(body.cc),bcc=[...new Set([...parseEmails(body.bcc),...mandatoryBcc(env)])],recipients=[...new Set([...to,...cc,...bcc])],subject=safeHeader(body.subject),plain=clean(body.text||body.plainText||body.body||''),html=clean(body.html||body.bodyHtml||body.body||'');if(!from)return json({ok:false,error:'invalid_from_address',message:`Sender must use the ${ALLOWED_FROM_DOMAIN} domain.`},400);if(!to.length||!subject||(!plain&&!html))return json({ok:false,error:'missing_required_fields',message:'Recipient, subject and message body are required.'},400);if(recipients.length>MAX_RECIPIENTS)return json({ok:false,error:'recipient_limit_exceeded',message:`Maximum ${MAX_RECIPIENTS} unique recipients are allowed.`},400);const id=crypto.randomUUID(),results=[];for(const recipient of recipients){try{const raw=await rawMessage(env,{from,fromName,to:recipient,subject,body});await env.EMAIL.send(new EmailMessage(from,recipient,raw));results.push({recipient,status:'SENT'})}catch(error){results.push({recipient,status:'FAILED',error:String(error?.message||error),code:clean(error?.code)||'EMAIL_SEND_FAILED'})}}const sent=results.filter(r=>r.status==='SENT').length,failed=results.length-sent,firstFailure=results.find(r=>r.status==='FAILED'),status=failed===0?'SENT':sent?'PARTIAL':'FAILED';return json({ok:failed===0,id,status,delivered:failed===0,sent,failed,results,message:firstFailure?firstFailure.error:'Message sent successfully.',error:firstFailure?.code||undefined,mandatoryBcc:mandatoryBcc(env),signature:SIGNATURE_VERSION,logo:'cid-inline'},failed===0?200:sent?207:502);}
function scheduleResponse(request){const path=pathOf(request);if(path===`${SCHEDULE_PREFIX}/list`&&request.method==='GET')return json({ok:true,items:[],message:'Scheduled mail is disabled in controlled mode.'});if(path===`${SCHEDULE_PREFIX}/cancel`&&request.method==='POST')return json({ok:false,error:'scheduled_mail_disabled',message:'Scheduled mail is disabled in controlled mode.'},409);if(path===SCHEDULE_PREFIX&&request.method==='POST')return json({ok:false,error:'scheduled_mail_disabled',message:'Scheduled mail is disabled in controlled mode.'},409);return json({ok:false,error:'not_found'},404);}
async function publicHtmlResponse(request,env){if(request.method!=='GET'&&request.method!=='HEAD')return null;const assetPath=PUBLIC_HTML_ROUTES.get(pathOf(request));if(!assetPath||!env.ASSETS?.fetch)return null;const target=new URL(assetPath,request.url);target.search='';const response=await env.ASSETS.fetch(new Request(target.toString(),{method:request.method,headers:request.headers}));if(response.status===404)return null;const headers=new Headers(response.headers);headers.delete('content-length');headers.delete('content-encoding');headers.set('content-type','text/html; charset=utf-8');headers.set('cache-control','public, max-age=120, stale-while-revalidate=300');headers.set('x-gnk-public-route-source','v18-explicit-static-asset');return new Response(request.method==='HEAD'?null:await response.text(),{status:response.status,statusText:response.statusText,headers});}
async function injectRelease(request,response){if(request.method!=='GET'||response.status!==200)return response;const type=String(response.headers.get('content-type')||'').toLowerCase();if(!type.includes('text/html'))return response;const path=pathOf(request);if(path.startsWith('/admin')||path.startsWith('/mail-studio')||path.startsWith('/email-status')||path.startsWith('/operator-dashboard'))return response;try{let html=await response.text();html=html.replace(/<script[^>]+release-completion-v1\.js[^>]*><\/script>/gi,'');html=html.includes('</body>')?html.replace('</body>',`${RELEASE_SCRIPT}</body>`):`${html}${RELEASE_SCRIPT}`;const headers=new Headers(response.headers);headers.delete('content-length');headers.delete('content-encoding');headers.set('content-type','text/html; charset=utf-8');headers.set('x-gnk-release-completion','v5-index-complete');return new Response(html,{status:response.status,statusText:response.statusText,headers})}catch{return response}}
export default{async fetch(request,env,ctx){const path=pathOf(request);if(SEND_PATHS.has(path)&&request.method==='POST')return sendRaw(request,env,ctx);if(path===SCHEDULE_PREFIX||path.startsWith(`${SCHEDULE_PREFIX}/`))return scheduleResponse(request);const publicHtml=await publicHtmlResponse(request,env);if(publicHtml)return injectRelease(request,publicHtml);return injectRelease(request,await app.fetch(request,env,ctx))},scheduled(event,env,ctx){if(typeof app.scheduled==='function')return app.scheduled(event,env,ctx)},async email(message,env,ctx){if(typeof app.email==='function')return app.email(message,env,ctx)}};